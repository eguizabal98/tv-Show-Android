CREATE TABLE Session_Id_Entity (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT NOT NULL
);

getSessionId:
SELECT *
FROM Session_Id_Entity LIMIT 1;

insertSessionId:
INSERT INTO Session_Id_Entity(id, session_id)
VALUES (?, ?);

clearSessionId:
DELETE FROM Session_Id_Entity;